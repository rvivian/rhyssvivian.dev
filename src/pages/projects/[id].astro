---
import { getCollection, render } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";

export async function getStaticPaths() {
  const projects = await getCollection("projects");

  return projects.map((project) => ({
    params: { id: project.id },
    props: { project },
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
---

<BaseLayout title={project.data.title} description={project.data.summary}>
  <article>
    <header class="border-b theme-border theme-surface">
      <div class="mx-auto max-w-4xl px-4 py-16 sm:px-6 lg:py-20">
        <p class="text-sm font-semibold uppercase tracking-[0.18em] theme-eyebrow">
          {project.data.category} / {project.data.year}
        </p>
        <h1 class="mt-4 text-4xl font-semibold tracking-normal theme-text sm:text-5xl">
          {project.data.title}
        </h1>
        <p class="mt-6 text-lg leading-8 theme-muted">{project.data.summary}</p>
        <div class="mt-6 flex flex-wrap gap-2">
          {project.data.tags.map((tag) => <span class="rounded-md px-2.5 py-1 text-xs font-medium theme-tag">{tag}</span>)}
        </div>
      </div>
    </header>

    <div class="mx-auto grid max-w-4xl gap-10 px-4 py-12 sm:px-6 lg:py-16">
      <section>
        <h2 class="text-xl font-semibold tracking-normal theme-text">Impact</h2>
        <ul class="mt-4 grid gap-3">
          {
            project.data.impact.map((item) => (
              <li class="rounded-lg border theme-card p-4 leading-7 theme-muted">{item}</li>
            ))
          }
        </ul>
      </section>
      <section class="content">
        <Content />
      </section>
    </div>
  </article>
</BaseLayout>
